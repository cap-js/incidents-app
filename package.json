{
  "name": "@capire/incidents",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": ">=8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-test": "*",
    "@cap-js/audit-logging": ">=0.8.3",
    "@cap-js/change-tracking": "^1.0.6",
    "@cap-js/attachments": "^2",
    "@cap-js/print": "file:../print",
    "@cap-js/sqlite": ">=1"
  },
  "scripts": {
    "watch": "cds watch",
    "start": "cds-serve",
    "jest": "npx jest --silent",
    "test": "chest",
    "in-cap-dev": "cd node_modules && rm -fr @sap/cds* @cap-js/db-service @cap-js/sqlite"
  },
  "sapux": [
    "app/incidents"
  ],
  "private": true
}
