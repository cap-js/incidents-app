_schema-version: 1.0.0
ID: incidents-testingMTX
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm i
        - npx cds build --production
modules:
  - name: incidents-testingMTX-testing-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: incidents-testingMTX-destination
      - name: incidents-testingMTX-auth
      - name: incidents-testingMTX-attachments
      - name: incidents-testingMTX-malware
      - name: incidents-testingMTX-db

  - name: incidents-testingMTX-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: incidents-testingMTX-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: app/
      requires:
        - name: incidentsincidents
          artifacts:
            - incidents.zip
          target-path: app/

  - name: incidentsincidents
    type: html5
    path: app/incidents
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: incidents-testingMTX-destinations
    type: com.sap.application.content
    requires:
      - name: incidents-testingMTX-auth
        parameters:
          service-key:
            name: incidents-testingMTX-auth-key
      - name: incidents-testingMTX-html5-repo-host
        parameters:
          service-key:
            name: incidents-testingMTX-html5-repo-host-key
      - name: srv-api
      - name: incidents-testingMTX-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: incidents-testingMTX-html5-repository
              ServiceInstanceName: incidents-testingMTX-html5-repo-host
              ServiceKeyName: incidents-testingMTX-html5-repo-host-key
              sap.cloud.service: incidents.service
            - Name: incidents-testingMTX-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: incidents-testingMTX-auth
              ServiceKeyName: incidents-testingMTX-auth-key
              sap.cloud.service: incidents.service

  - name: incidents-testingMTX-mtx
    type: nodejs
    path: gen/mtx/sidecar
    build-parameters:
      builder: npm-ci
    parameters:
      instances: 1
      memory: 256M
      disk-quota: 512M
    provides:
      - name: mtx-api
        properties:
          mtx-url: ${default-url}
    requires:
      - name: incidents-testingMTX-registry
      - name: incidents-testingMTX-destination
      - name: incidents-testingMTX-db
      - name: incidents-testingMTX-html5-runtime
      - name: incidents-testingMTX-html5-repo-host
      - name: incidents-testingMTX-auth

resources:
  - name: incidents-testingMTX-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: incidents-testingMTX-srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
          subaccount:
            existing_destinations_policy: update
            destinations:
              - Name: incidents-testingMTX-workzone-runtime
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                URL: https://${org}.launchpad.${default-domain}
                CEP.HTML5contentprovider: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
  - name: incidents-testingMTX-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: incidents-testingMTX-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: incidents-testingMTX-${org}-${space}
        tenant-mode: shared
  - name: incidents-testingMTX-attachments
    type: org.cloudfoundry.managed-service
    parameters:
      service: objectstore
      service-plan: s3-standard
  - name: incidents-testingMTX-malware
    type: org.cloudfoundry.managed-service
    parameters:
      service: malware-scanner
      service-plan: clamav
  - name: incidents-testingMTX-registry
    type: org.cloudfoundry.managed-service
    requires:
      - name: mtx-api
    parameters:
      service: saas-registry
      service-plan: application
      config:
        xsappname: incidents-testingMTX-${org}-${space}
        appName: incidents-testingMTX-${org}-${space}
        displayName: incidents
        description: A simple CAP project.
        category: 'Category'
        appUrls:
          getDependencies: ~{mtx-api/mtx-url}/-/cds/saas-provisioning/dependencies
          onSubscription: ~{mtx-api/mtx-url}/-/cds/saas-provisioning/tenant/{tenantId}
          onSubscriptionAsync: true
          onUnSubscriptionAsync: true
          onUpdateDependenciesAsync: true
          callbackTimeoutMillis: 300000 # Increase if your deployments are taking longer than that
  - name: incidents-testingMTX-db
    type: org.cloudfoundry.managed-service
    parameters:
      service: service-manager
      service-plan: container
  - name: incidents-testingMTX-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
