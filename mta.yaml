_schema-version: 3.3.0
ID: capire.incidents
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm i
        - npx cds build --production
modules:
  - name: incidents-testing-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: incidents-testing-destination
      - name: incidents-testing-db
      - name: incidents-testing-auth
      - name: incidents-testing-attachments
      - name: incidents-testing-malware
 
  - name: incidents-testing-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: incidents-testing-db
 
  - name: incidents-testing-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: incidents-testing-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: app/
      requires:
        - name: incidentsincidents
          artifacts:
            - incidents.zip
          target-path: app/
 
  - name: incidentsincidents
    type: html5
    path: app/incidents
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
 
  - name: incidents-testing-destinations
    type: com.sap.application.content
    requires:
      - name: incidents-testing-auth
        parameters:
          service-key:
            name: incidents-testing-auth-key
      - name: incidents-testing-html5-repo-host
        parameters:
          service-key:
            name: incidents-testing-html5-repo-host-key
      - name: srv-api
      - name: incidents-testing-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: incidents-testing-html5-repository
              ServiceInstanceName: incidents-testing-html5-repo-host
              ServiceKeyName: incidents-testing-html5-repo-host-key
              sap.cloud.service: incidents.service
            - Name: incidents-testing-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: incidents-testing-auth
              ServiceKeyName: incidents-testing-auth-key
              sap.cloud.service: incidents.service
 
resources:
  - name: incidents-testing-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: incidents-testing-srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
  - name: incidents-testing-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: incidents-testing-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: incidents-testing-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: incidents-testing-${org}-${space}
        tenant-mode: dedicated
  - name: incidents-testing-attachments
    type: org.cloudfoundry.managed-service
    parameters:
      service: objectstore
      service-plan: s3-standard
  - name: incidents-testing-malware
    type: org.cloudfoundry.managed-service
    parameters:
      service: malware-scanner
      service-plan: clamav