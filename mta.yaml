_schema-version: 1.0.0
ID: incidents-testing
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm i
        - npx cds build --production
modules:
  - name: incidents-testing-testing-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: incidents-testing-testing-destination
      - name: incidents-testing-auth
      - name: incidents-testing-attachments
      - name: incidents-testing-malware
      - name: incidents-testing-db

  - name: incidents-testing-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: incidents-testing-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: app/
      requires:
        - name: incidentsincidents
          artifacts:
            - incidents.zip
          target-path: app/

  - name: incidentsincidents
    type: html5
    path: app/incidents
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: incidents-testing-destinations
    type: com.sap.application.content
    requires:
      - name: incidents-testing-auth
        parameters:
          service-key:
            name: incidents-testing-auth-key
      - name: incidents-testing-html5-repo-host
        parameters:
          service-key:
            name: incidents-testing-html5-repo-host-key
      - name: srv-api
      - name: incidents-testing-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: incidents-testing-html5-repository
              ServiceInstanceName: incidents-testing-html5-repo-host
              ServiceKeyName: incidents-testing-html5-repo-host-key
              sap.cloud.service: incidents.service
            - Name: incidents-testing-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: incidents-testing-auth
              ServiceKeyName: incidents-testing-auth-key
              sap.cloud.service: incidents.service

  - name: incidents-testing-mtx
    type: nodejs
    path: gen/mtx/sidecar
    build-parameters:
      builder: npm-ci
    parameters:
      instances: 1
      memory: 256M
      disk-quota: 512M
    provides:
      - name: mtx-api
        properties:
          mtx-url: ${default-url}
    requires:
      - name: incidents-testing-registry
      - name: incidents-testing-destination
      - name: incidents-testing-db
      - name: incidents-testing-html5-runtime
      - name: incidents-testing-html5-repo-host
      - name: incidents-testing-auth

resources:
  - name: incidents-testing-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: incidents-testing-srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
          subaccount:
            existing_destinations_policy: update
            destinations:
              - Name: incidents-testing-workzone-runtime
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                URL: https://${org}.launchpad.${default-domain}
                CEP.HTML5contentprovider: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
  - name: incidents-testing-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: incidents-testing-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: incidents-testing-${org}-${space}
        tenant-mode: shared
  - name: incidents-testing-attachments
    type: org.cloudfoundry.managed-service
    parameters:
      service: objectstore
      service-plan: s3-standard
  - name: incidents-testing-malware
    type: org.cloudfoundry.managed-service
    parameters:
      service: malware-scanner
      service-plan: clamav
  - name: incidents-testing-registry
    type: org.cloudfoundry.managed-service
    requires:
      - name: mtx-api
    parameters:
      service: saas-registry
      service-plan: application
      config:
        xsappname: incidents-testing-${org}-${space}
        appName: incidents-testing-${org}-${space}
        displayName: incidents
        description: A simple CAP project.
        category: 'Category'
        appUrls:
          getDependencies: ~{mtx-api/mtx-url}/-/cds/saas-provisioning/dependencies
          onSubscription: ~{mtx-api/mtx-url}/-/cds/saas-provisioning/tenant/{tenantId}
          onSubscriptionAsync: true
          onUnSubscriptionAsync: true
          onUpdateDependenciesAsync: true
          callbackTimeoutMillis: 300000 # Increase if your deployments are taking longer than that
  - name: incidents-testing-db
    type: org.cloudfoundry.managed-service
    parameters:
      service: service-manager
      service-plan: container
  - name: incidents-testing-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
